/**
 * SQL persistence adapter (MySQL via JPA / Hibernate).
 *
 * <p>This package will contain:
 * <ul>
 *   <li>JPA {@code @Entity} classes that map domain models to relational tables</li>
 *   <li>Spring Data {@code JpaRepository} interfaces</li>
 *   <li>Adapter classes that implement the output ports
 *       ({@code ClientRepositoryPort}, {@code BankAccountRepositoryPort},
 *        {@code LoanRepositoryPort}, {@code TransferRepositoryPort}, etc.)</li>
 * </ul>
 */
package co.edu.tdea.bank.infrastructure.adapter.sql;
